// git : code의 version 관리

// git init : git 시작, git status : git의 상태 표시 -> 표시(staging area)

// working dir : 아무것도 안한 상태, git add : working dir -> staging area(node modules은 안 올려도 됨) (2)

// library file은 빼고 올리기 -> .gitignore : staging area에 저장하지 않을 수 있음(node_modules) (1)

